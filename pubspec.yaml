name: date_snap

description: A lightweight Flutter package for localized date formatting and relative time calculation. Supports English, Bengali, and Arabic.

version: 1.0.0

homepage: https://github.com/oxfaysal/date_snap
issues: https://github.com/oxfaysal/date_snap/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0